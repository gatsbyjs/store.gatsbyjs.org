import React from 'react';
import ProductListings from './ProductListings';
import { Heading, Lede } from '../shared/Typography';

export default () => (
  <>
    <Heading>Get Gatsby Swag</Heading>
    <Lede>
      TKTK write a fun description with a CTA for non-contributors to get
      involved for free swag.
    </Lede>
    <ProductListings />
  </>
);
